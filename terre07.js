const { argv } = require("process");

const characters = String(argv[2]);

let charactersTotal = "";
for (let i = characters[0]; (i) => characters.length; characters) {
  charactersTotal = characters[i];
}
console.log(charactersTotal);
